import { TextValidationDirective } from './text-validation.directive';

describe('TextValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new TextValidationDirective();
    expect(directive).toBeTruthy();
  });
});
